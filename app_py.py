# -*- coding: utf-8 -*-
"""app.py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/14lDaYOiDvwwKIRzkl3MnTwjZH1562Sie

Upload your Dataset
"""

from google.colab import files
uploaded = files.upload()

"""Install Required Packages"""

!pip install joblib scikit-learn pandas

"""Load the Dataset and Train the Model"""

import pandas as pd
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
import joblib

# Load dataset
df = pd.read_csv("glass.csv")
X = df.drop("Type", axis=1)
y = df["Type"]

# Preprocessing
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# Train/test split
X_train, X_test, y_train, y_test = train_test_split(X_scaled, y, test_size=0.2, random_state=42)

# Train model
model = RandomForestClassifier()
model.fit(X_train, y_train)

# Save model and scaler
joblib.dump(model, "glass_model.pkl")
joblib.dump(scaler, "scaler.pkl")

"""Download the Model Files to Your Local Machine"""

files.download("glass_model.pkl")
files.download("scaler.pkl")
files.download("glass.csv")  # Optional, if you want to reuse it in the Streamlit app